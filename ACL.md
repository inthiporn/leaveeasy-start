# ACL.md — ตารางสิทธิ์การใช้งาน LeaveEasy

ตารางนี้สรุปสิทธิ์ของผู้ใช้ 3 บทบาทตามที่กำหนดไว้ใน `leaveeasy-spec.md` (§2)

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · ดูใบลาของคนอื่น · แก้ไขประเภทการลา |
| ✅ **หัวหน้า** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไขประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · ดูแดชบอร์ดสรุปทั้งระบบ | — |

## ⚠️ สถานะการบังคับใช้จริงในโค้ดตอนนี้

ตารางข้างบนคือ**สิทธิ์ที่ตั้งใจไว้ตามสเปก** แต่ ณ ตอนนี้ **ยังไม่มีการบังคับสิทธิ์ตามบทบาทในระบบจริงเลย** — ทุกคนที่ล็อกอินแล้ว (ไม่ว่า role อะไร) ยังเห็น/แก้/ลบใบลาได้ทุกใบเหมือนกันหมด เพราะ:

- Firestore Security Rules ตอนนี้มีแค่กฎขั้นต่ำ **"ต้องล็อกอินก่อน ถึงอ่าน/เขียนได้"** (`request.auth != null`) ยังไม่มีกฎแยกตาม `role`
- โค้ดฝั่งหน้าเว็บ (`js/*.js`) ก็ยังไม่มีจุดไหนเช็คค่า `role` เพื่อซ่อนปุ่มหรือกันการกระทำเลย

การบังคับสิทธิ์ตามตารางนี้จริงเป็นงานของ **สัปดาห์ที่ 8** ตามแผนใน `leaveeasy-spec.md` §8 — ทั้ง Security Rules รายบทบาท และการซ่อน/ปิดปุ่มในหน้าจอตามสิทธิ์
